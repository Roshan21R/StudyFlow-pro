import 'package:flutter/material.dart';

class VoiceNotesScreen extends StatelessWidget {
  
  
  
  
  const VoiceNotesScreen({
    Key? key,
    
    
    
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Voice Notes')),
      body: const Center(child: Text('VoiceNotes - Coming Soon!')),
    );
  }
}
