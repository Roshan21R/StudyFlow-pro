import 'package:flutter/material.dart';

class PdfScannerScreen extends StatelessWidget {
  
  
  
  
  const PdfScannerScreen({
    Key? key,
    
    
    
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Pdf Scanner')),
      body: const Center(child: Text('PdfScanner - Coming Soon!')),
    );
  }
}
